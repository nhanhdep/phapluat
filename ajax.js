/**
 * AJAX SCRIPTS.
 *
 * @ajax URL: myAjax.ajaxurl = admin_url( 'admin-ajax.php' )
 * @ajax NONCE: myAjax.ajaxnonce = wp_create_nonce( 'ajax-nonce' )
 *
 **/

( function( $ ) {

    /** == AJAX FUNCTION NAME == **/

 
} )( jQuery );